rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Client SDK reads/writes are denied by default.
    // Server access should happen through Firebase Admin SDK only.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
